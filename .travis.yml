language: python

services:
  - docker

install: skip

before_script:
- docker pull benjaminbrelje/mdolabfull:latest
- ls

script:
- docker run -v /mdolab/pygeo:/root/test/pygeo --rm benjaminbrelje/mdolabfull:latest /bin/sh -c ". /root/.bashrc && cd /root/test && python -m pytest pygeo/tests/reg_tests"